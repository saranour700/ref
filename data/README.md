
# Data Directory

This directory is reserved for local and generated pipeline data.

Data files are not committed to the repository by default.

Examples include:

* Raw ingestion outputs
* Intermediate datasets
* Validation outputs
* Local DuckDB files
* Generated pipeline artifacts

Large datasets and source data should be stored outside the Git repository when appropriate.
