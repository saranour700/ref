[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "reference-db"
version = "0.1.0"
description = "Reference Database pipeline for Open Food Facts Canada"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
